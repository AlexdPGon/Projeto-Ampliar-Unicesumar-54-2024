/*
    Faça um programa que leia um número inteiro e imprima o número com seus dígitos invertidos.
    Se o número inteiro for zero, deverá pedir ao usuário que entre com um valor diferente.
    Exemplo: 1234 vira 4321.
*/

#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main () {
    setlocale(LC_ALL, "Portuguese");
    system("clear");

    int numero, resto, inverterNum = 0;

    do {
    printf("Digite um número (Não pode ser zero): ");
    scanf("%d", &numero);
    getchar();

    } while (numero == 0);

    while (numero != 0) {
        resto = numero % 10;
        inverterNum = inverterNum * 10 + resto;
        numero = numero / 10;
    }

    printf("O número invertido é: %d\n", inverterNum);

    return 0;
}
